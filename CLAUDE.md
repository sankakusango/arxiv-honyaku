# 文章ルール

- 日本語の句読点は `.` (ピリオド) と `,` (カンマ) を使う. `。` `、` は使わない.
- このルールはコメント, ドキュメント, README, スクリプト内のメッセージなど, レポジトリ内のすべての文章に適用する.

# コーディングルール

- タプルは, それが合理的な場合 (固定長で意味が自明, 関数の複数戻り値の即時アンパック, ハッシュキーとして使うなど) を除き極力使わない.
- 複数の値を1つにまとめたいときは, dataclass などで名前空間としてアクセスできる形, または辞書を優先する. インデックスや位置で意味を覚えさせない.
